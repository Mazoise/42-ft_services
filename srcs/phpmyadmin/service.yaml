apiVersion: v1
kind: Service
metadata:
  labels:
    app: phpmyadmin
  name: phpmyadmin
  annotations:
    metallb.universe.tf/address-pool: my-ip-space
spec:
  ports:
	- name: phpmyadmin
	  port: 5050
	  targetPort: 5050
  type: LoadBalancer
  selector:
    app: phpmyadmin