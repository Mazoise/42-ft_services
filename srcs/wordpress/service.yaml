apiVersion: v1
kind: Service
metadata:
  labels:
    app: wordpress
  name: wordpress
  annotations:
    metallb.universe.tf/address-pool: my-ip-space
spec:
  ports:
  - name: wordpress
    port: 5050
    targetPort: 5050
  type: LoadBalancer
  selector:
    app: wordpress